describe('Group Admin Pages', () => {

  // before(() => {
  //   cy.apiCreateUser(Anna);
  // });

  // beforeEach(() => {
  //   cy.signInAs(Anna);
  // });

  it ('my test', () => {
    cy.apiSignInAs("Anna");
  });
});
